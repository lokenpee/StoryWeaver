已完成修复：

在世界书条目头部真正渲染了删除按钮（🗑️），这样你在“查看世界书”里能直接点删。
renderer.js:204

实现并导出了删除世界书条目的真实逻辑，删除时会按显示名解析到实际来源条目（包含卷模式下的 _卷N 映射），并清理当前卷对应的条目配置残留。
mergeWorkflowService.js:1203

现有删除事件绑定链路已可用，不需要你再额外配置。
worldbookView.js:196
createRuntimeBridges.js:38

校验结果：

相关文件静态错误检查均通过，无语法错误。
你现在可以这样用：

先跑完 TXT 转世界书。
打开“生成结果” -> “查看世界书”。
在每条条目标题行右侧点击 🗑️ 删除。
确认后会立刻从当前视图移除并刷新。

打开“查看世界书”。
点击底部“🧺 多选删除”进入多选模式。
在条目标题行点击“☑️/✅”选择条目（会有绿色高亮）。
点击“🗑️ 删除已选 (N)”一次性删除。

做这个这个删除世界树条目的时候注意啊，这个多选删除，之前就是做完了这个功能以后，点击那个世界书条目进行勾选的时候，他会把那个那个那个那个世界书类型的那个栏给收起来，然后我就要再勾选其他条目的时候，还得再点击那个栏，让他弹开，然后在勾选的时候，那个栏就会收起来。你这个得到时候注意一下。
然后他记得美化一下下面这个让他排版UI啊，记得让它排版整齐一点。


你要的不是后置整理，而是：

每章主 API 返回 JSON。
先存本地结构化对象。
同名条目在本地按字段合并。
同字段同值跳过；同字段异值融合。
再写入世界书。
最后整理条目才是兜底，不是主战场。
这个就是你说的“源头结构化去重”，完全可做，而且比“纯提示词”稳很多。

我已经把计划按你这套逻辑重写好了，关键改造点如下：

源头融合入口在 worldbookService.js:78 和 worldbookService.js:96。
这里把当前内容拼接去重改成字段级融合函数。

字段融合工具放在 nameNormalizationService.js:77 附近扩展，或拆新函数供 worldbookService 调用。
规则就是你说的：同字段同值跳过、同字段异值合并、自由文本去重。

主 API 仍然一次，不增加调用。
链路保持 processingService.js:2022 -> processingService.js:2275。

整理条目改成双层防线：
先本地函数去重融合，再调 API，再本地兜底一次。
对应点是 mergeWorkflowService.js:107。